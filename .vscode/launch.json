{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Portfolio",
            "type": "msedge",
            "request": "launch",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}
